<div class="main">
  <div class="text_container">
    Lazy loading is a design pattern commonly used in computer programming to defer initialization of an object until the point at which it is needed. It can contribute to efficiency in the program's operation if properly and appropriately used. The opposite of lazy loading is eager loading.
  </div>


  <%= image_tag '', class: 'lazy-image', data: { src: asset_path('lazy1.webp') } %>

  <div class="text_container">
    Lazy loading is a design pattern commonly used in computer programming to defer initialization of an object until the point at which it is needed. It can contribute to efficiency in the program's operation if properly and appropriately used. The opposite of lazy loading is eager loading.
  </div>


  <%= image_tag '', class: 'lazy-image', data: { src: asset_path('lazy2.webp') } %>

  <div class="text_container">
    Lazy loading is a design pattern commonly used in computer programming to defer initialization of an object until the point at which it is needed. It can contribute to efficiency in the program's operation if properly and appropriately used. The opposite of lazy loading is eager loading.
  </div>


  <%= image_tag '', class: 'lazy-image', data: { src: asset_path('lazy3.webp') } %>

  <div class="text_container">
    Lazy loading is a design pattern commonly used in computer programming to defer initialization of an object until the point at which it is needed. It can contribute to efficiency in the program's operation if properly and appropriately used. The opposite of lazy loading is eager loading.
  </div>


  <%= image_tag '', class: 'lazy-image', data: { src: asset_path('lazy4.webp') } %>

  <div class="text_container">
    Lazy loading is a design pattern commonly used in computer programming to defer initialization of an object until the point at which it is needed. It can contribute to efficiency in the program's operation if properly and appropriately used. The opposite of lazy loading is eager loading.
  </div>


  <%= image_tag '', class: 'lazy-image', data: { src: asset_path('lazy5.webp') } %>

  <div class="text_container">
    Lazy loading is a design pattern commonly used in computer programming to defer initialization of an object until the point at which it is needed. It can contribute to efficiency in the program's operation if properly and appropriately used. The opposite of lazy loading is eager loading.
  </div>


  <%= image_tag '', class: 'lazy-image', data: { src: asset_path('lazy6.webp') } %>
  <div class="text_container">
    Lazy loading is a design pattern commonly used in computer programming to defer initialization of an object until the point at which it is needed. It can contribute to efficiency in the program's operation if properly and appropriately used. The opposite of lazy loading is eager loading.
  </div>

  <%= image_tag '', class: 'lazy-image', data: { src: asset_path('lazy7.webp') } %>
  <div class="text_container">
    Lazy loading is a design pattern commonly used in computer programming to defer initialization of an object until the point at which it is needed. It can contribute to efficiency in the program's operation if properly and appropriately used. The opposite of lazy loading is eager loading.
  </div>

  <%= image_tag '', class: 'lazy-image', data: { src: asset_path('lazy8.webp') } %>
  <div class="text_container">
    Lazy loading is a design pattern commonly used in computer programming to defer initialization of an object until the point at which it is needed. It can contribute to efficiency in the program's operation if properly and appropriately used. The opposite of lazy loading is eager loading.
  </div>

  <%= image_tag '', class: 'lazy-image', data: { src: asset_path('lazy9.webp') } %>
  <div class="text_container">
    Lazy loading is a design pattern commonly used in computer programming to defer initialization of an object until the point at which it is needed. It can contribute to efficiency in the program's operation if properly and appropriately used. The opposite of lazy loading is eager loading.
  </div>

  <%= image_tag '', class: 'lazy-image', data: { src: asset_path('lazy10.webp') } %>
  <div class="text_container">
    Lazy loading is a design pattern commonly used in computer programming to defer initialization of an object until the point at which it is needed. It can contribute to efficiency in the program's operation if properly and appropriately used. The opposite of lazy loading is eager loading.
  </div>

  <%= image_tag '', class: 'lazy-image', data: { src: asset_path('lazy11.webp') } %>
</div>
